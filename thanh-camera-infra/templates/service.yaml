apiVersion: v1
kind: Service
metadata:
  name: {{ include "thanh-camera-infra.fullname" . }}
  labels:
    {{- include "thanh-camera-infra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "thanh-camera-infra.selectorLabels" . | nindent 4 }}
