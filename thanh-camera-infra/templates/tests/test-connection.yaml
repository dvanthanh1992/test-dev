apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thanh-camera-infra.fullname" . }}-test-connection"
  labels:
    {{- include "thanh-camera-infra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "thanh-camera-infra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
